# Uncomment the next line to define a global platform for your project
# platform :ios, '9.0'

target 'Vane' do
  # Comment the next line if you don't want to use dynamic frameworks
  use_frameworks!

  # Pods for Vane
  
  pod 'RealmSwift'
  pod "TinyConstraints"
  pod "Charts"

  target 'VaneTests' do
    inherit! :search_paths
    # Pods for testing
  end

  target 'VaneUITests' do
    # Pods for testing
  end

end
